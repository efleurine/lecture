import React from "react";
import { View, Text } from "react-native-ui-lib";

export default class Openning extends React.PureComponent {
  render() {
    return (
      <View>
        <Text>Open</Text>
      </View>
    );
  }
}
