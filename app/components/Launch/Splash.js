import React from "react";
import { View, Text } from "react-native";

export default class Splash extends React.PureComponent {
  render() {
    return (
      <View>
        <Text>Tempo splash screen</Text>
      </View>
    );
  }
}
